// Explicitly write out the steps you need to take.

// this forces us to think about the code you'll write before we write it and help us
// catch any lingering conceptual issues or misunderstandings before we dive in and have
// to worry about details (like syntax) as well.

function charCount(str) {
    // make object to return
    // loop over string, for each char...
        // are there conditions on what chars are we counting 
        // if the char is not in the object, add it and set value to 1
        // if the char is key in the object increment value
        // skip chars that do not meet the conditions if any
    // return object at end
}